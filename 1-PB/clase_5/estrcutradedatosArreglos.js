"use strict";
exports.__esModule = true;
var rls = require("readline-sync");
var num = new Array(5);
for (var i = 0; i < 5; i++) {
    num[1] - rls.questionInt("ingrese un numero para almacenar");
}
